# models.py is no longer used.
# This project has been migrated from SQLAlchemy ORM to the Supabase Python SDK.
# All database tables are managed directly through Supabase.
# Table definitions exist in the Supabase dashboard (PostgreSQL).
